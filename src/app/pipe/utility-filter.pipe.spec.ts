import { UtilityFilterPipe } from './utility-filter.pipe';

describe('UtilityFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new UtilityFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
